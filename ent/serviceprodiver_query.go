// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database-concurrency/ent/predicate"
	"database-concurrency/ent/serviceprodiver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// ServiceProdiverQuery is the builder for querying ServiceProdiver entities.
type ServiceProdiverQuery struct {
	config
	limit      *int
	offset     *int
	unique     *bool
	order      []OrderFunc
	fields     []string
	predicates []predicate.ServiceProdiver
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ServiceProdiverQuery builder.
func (spq *ServiceProdiverQuery) Where(ps ...predicate.ServiceProdiver) *ServiceProdiverQuery {
	spq.predicates = append(spq.predicates, ps...)
	return spq
}

// Limit adds a limit step to the query.
func (spq *ServiceProdiverQuery) Limit(limit int) *ServiceProdiverQuery {
	spq.limit = &limit
	return spq
}

// Offset adds an offset step to the query.
func (spq *ServiceProdiverQuery) Offset(offset int) *ServiceProdiverQuery {
	spq.offset = &offset
	return spq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (spq *ServiceProdiverQuery) Unique(unique bool) *ServiceProdiverQuery {
	spq.unique = &unique
	return spq
}

// Order adds an order step to the query.
func (spq *ServiceProdiverQuery) Order(o ...OrderFunc) *ServiceProdiverQuery {
	spq.order = append(spq.order, o...)
	return spq
}

// First returns the first ServiceProdiver entity from the query.
// Returns a *NotFoundError when no ServiceProdiver was found.
func (spq *ServiceProdiverQuery) First(ctx context.Context) (*ServiceProdiver, error) {
	nodes, err := spq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{serviceprodiver.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (spq *ServiceProdiverQuery) FirstX(ctx context.Context) *ServiceProdiver {
	node, err := spq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ServiceProdiver ID from the query.
// Returns a *NotFoundError when no ServiceProdiver ID was found.
func (spq *ServiceProdiverQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = spq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{serviceprodiver.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (spq *ServiceProdiverQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := spq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ServiceProdiver entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ServiceProdiver entity is found.
// Returns a *NotFoundError when no ServiceProdiver entities are found.
func (spq *ServiceProdiverQuery) Only(ctx context.Context) (*ServiceProdiver, error) {
	nodes, err := spq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{serviceprodiver.Label}
	default:
		return nil, &NotSingularError{serviceprodiver.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (spq *ServiceProdiverQuery) OnlyX(ctx context.Context) *ServiceProdiver {
	node, err := spq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ServiceProdiver ID in the query.
// Returns a *NotSingularError when more than one ServiceProdiver ID is found.
// Returns a *NotFoundError when no entities are found.
func (spq *ServiceProdiverQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = spq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{serviceprodiver.Label}
	default:
		err = &NotSingularError{serviceprodiver.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (spq *ServiceProdiverQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := spq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ServiceProdivers.
func (spq *ServiceProdiverQuery) All(ctx context.Context) ([]*ServiceProdiver, error) {
	if err := spq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return spq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (spq *ServiceProdiverQuery) AllX(ctx context.Context) []*ServiceProdiver {
	nodes, err := spq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ServiceProdiver IDs.
func (spq *ServiceProdiverQuery) IDs(ctx context.Context) ([]uuid.UUID, error) {
	var ids []uuid.UUID
	if err := spq.Select(serviceprodiver.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (spq *ServiceProdiverQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := spq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (spq *ServiceProdiverQuery) Count(ctx context.Context) (int, error) {
	if err := spq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return spq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (spq *ServiceProdiverQuery) CountX(ctx context.Context) int {
	count, err := spq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (spq *ServiceProdiverQuery) Exist(ctx context.Context) (bool, error) {
	if err := spq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return spq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (spq *ServiceProdiverQuery) ExistX(ctx context.Context) bool {
	exist, err := spq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ServiceProdiverQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (spq *ServiceProdiverQuery) Clone() *ServiceProdiverQuery {
	if spq == nil {
		return nil
	}
	return &ServiceProdiverQuery{
		config:     spq.config,
		limit:      spq.limit,
		offset:     spq.offset,
		order:      append([]OrderFunc{}, spq.order...),
		predicates: append([]predicate.ServiceProdiver{}, spq.predicates...),
		// clone intermediate query.
		sql:    spq.sql.Clone(),
		path:   spq.path,
		unique: spq.unique,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ServiceProdiver.Query().
//		GroupBy(serviceprodiver.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (spq *ServiceProdiverQuery) GroupBy(field string, fields ...string) *ServiceProdiverGroupBy {
	grbuild := &ServiceProdiverGroupBy{config: spq.config}
	grbuild.fields = append([]string{field}, fields...)
	grbuild.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := spq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return spq.sqlQuery(ctx), nil
	}
	grbuild.label = serviceprodiver.Label
	grbuild.flds, grbuild.scan = &grbuild.fields, grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.ServiceProdiver.Query().
//		Select(serviceprodiver.FieldName).
//		Scan(ctx, &v)
//
func (spq *ServiceProdiverQuery) Select(fields ...string) *ServiceProdiverSelect {
	spq.fields = append(spq.fields, fields...)
	selbuild := &ServiceProdiverSelect{ServiceProdiverQuery: spq}
	selbuild.label = serviceprodiver.Label
	selbuild.flds, selbuild.scan = &spq.fields, selbuild.Scan
	return selbuild
}

// Aggregate returns a ServiceProdiverSelect configured with the given aggregations.
func (spq *ServiceProdiverQuery) Aggregate(fns ...AggregateFunc) *ServiceProdiverSelect {
	return spq.Select().Aggregate(fns...)
}

func (spq *ServiceProdiverQuery) prepareQuery(ctx context.Context) error {
	for _, f := range spq.fields {
		if !serviceprodiver.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if spq.path != nil {
		prev, err := spq.path(ctx)
		if err != nil {
			return err
		}
		spq.sql = prev
	}
	return nil
}

func (spq *ServiceProdiverQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ServiceProdiver, error) {
	var (
		nodes = []*ServiceProdiver{}
		_spec = spq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ServiceProdiver).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ServiceProdiver{config: spq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, spq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (spq *ServiceProdiverQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := spq.querySpec()
	_spec.Node.Columns = spq.fields
	if len(spq.fields) > 0 {
		_spec.Unique = spq.unique != nil && *spq.unique
	}
	return sqlgraph.CountNodes(ctx, spq.driver, _spec)
}

func (spq *ServiceProdiverQuery) sqlExist(ctx context.Context) (bool, error) {
	switch _, err := spq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

func (spq *ServiceProdiverQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   serviceprodiver.Table,
			Columns: serviceprodiver.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeUUID,
				Column: serviceprodiver.FieldID,
			},
		},
		From:   spq.sql,
		Unique: true,
	}
	if unique := spq.unique; unique != nil {
		_spec.Unique = *unique
	}
	if fields := spq.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, serviceprodiver.FieldID)
		for i := range fields {
			if fields[i] != serviceprodiver.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := spq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := spq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := spq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := spq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (spq *ServiceProdiverQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(spq.driver.Dialect())
	t1 := builder.Table(serviceprodiver.Table)
	columns := spq.fields
	if len(columns) == 0 {
		columns = serviceprodiver.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if spq.sql != nil {
		selector = spq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if spq.unique != nil && *spq.unique {
		selector.Distinct()
	}
	for _, p := range spq.predicates {
		p(selector)
	}
	for _, p := range spq.order {
		p(selector)
	}
	if offset := spq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := spq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ServiceProdiverGroupBy is the group-by builder for ServiceProdiver entities.
type ServiceProdiverGroupBy struct {
	config
	selector
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (spgb *ServiceProdiverGroupBy) Aggregate(fns ...AggregateFunc) *ServiceProdiverGroupBy {
	spgb.fns = append(spgb.fns, fns...)
	return spgb
}

// Scan applies the group-by query and scans the result into the given value.
func (spgb *ServiceProdiverGroupBy) Scan(ctx context.Context, v any) error {
	query, err := spgb.path(ctx)
	if err != nil {
		return err
	}
	spgb.sql = query
	return spgb.sqlScan(ctx, v)
}

func (spgb *ServiceProdiverGroupBy) sqlScan(ctx context.Context, v any) error {
	for _, f := range spgb.fields {
		if !serviceprodiver.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := spgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := spgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (spgb *ServiceProdiverGroupBy) sqlQuery() *sql.Selector {
	selector := spgb.sql.Select()
	aggregation := make([]string, 0, len(spgb.fns))
	for _, fn := range spgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(spgb.fields)+len(spgb.fns))
		for _, f := range spgb.fields {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	return selector.GroupBy(selector.Columns(spgb.fields...)...)
}

// ServiceProdiverSelect is the builder for selecting fields of ServiceProdiver entities.
type ServiceProdiverSelect struct {
	*ServiceProdiverQuery
	selector
	// intermediate query (i.e. traversal path).
	sql *sql.Selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sps *ServiceProdiverSelect) Aggregate(fns ...AggregateFunc) *ServiceProdiverSelect {
	sps.fns = append(sps.fns, fns...)
	return sps
}

// Scan applies the selector query and scans the result into the given value.
func (sps *ServiceProdiverSelect) Scan(ctx context.Context, v any) error {
	if err := sps.prepareQuery(ctx); err != nil {
		return err
	}
	sps.sql = sps.ServiceProdiverQuery.sqlQuery(ctx)
	return sps.sqlScan(ctx, v)
}

func (sps *ServiceProdiverSelect) sqlScan(ctx context.Context, v any) error {
	aggregation := make([]string, 0, len(sps.fns))
	for _, fn := range sps.fns {
		aggregation = append(aggregation, fn(sps.sql))
	}
	switch n := len(*sps.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		sps.sql.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		sps.sql.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := sps.sql.Query()
	if err := sps.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
